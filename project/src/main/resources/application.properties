spring.application.name=project
spring.datasource.url=jdbc:mysql://localhost:3306/BD_FEMTech_TheAngels?createDatabaseIfNotExist=true
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

spring.datasource.username=root
spring.datasource.password=25135Pao..

# Para mantener privacidad, usuario y contraseña se configuran como variables de entorno
# Arriba en el run del programa "Edit Configurations" -> "Modify options" -> Override configuration properties
# Agregamos: spring.datasource.username con el valor del usuario de MySQL
# Agregamos: spring.datasource.password con el valor de la contraseña

# Para visualizar los comandos SQL que se ejecutan
spring.jpa.show-sql=true

# Indicamos qué operación se va a realizar con la BD al ejecutar la APP
spring.jpa.hibernate.ddl-auto=update

# Configuramos el puerto en el que se va a desplegar nuestra APP
server.port=8080
# Retrasa la inicialización de los datos hasta que JPA termine
spring.jpa.defer-datasource-initialization=true